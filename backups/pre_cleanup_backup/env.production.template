# ========================================
# PRODUKCJA - Netlify (Neon Postgres)
# Netlify dostarcza zmienną DATABASE_URL wskazującą na bazę Neon Postgres.
# Jeśli potrzebujesz nadpisać lokalnie, wstaw tutaj connection string do Neon.
# Legacy (nieużywane):
# DATABASE_URL="mysql://admin22505_4558816:Kie%40%21st78ar%3FX@22505.m.tld.pl:3306/baza22505_4558816"

# JWT Secret (WAŻNE: wygeneruj nowy dla produkcji!)
JWT_SECRET="wlasniewski-fotograf-jwt-secret-2024-production"

# Base URL
NEXT_PUBLIC_BASE_URL="https://wlasniewski.pl"

# External API (optional)
# Set STRAPI_API_URL to point builds at a reachable CMS endpoint.
# Example: STRAPI_API_URL="https://cms.example.com/api"
STRAPI_API_URL=""

# SMTP (skonfiguruj w CyberFolks)
SMTP_HOST="mail.wlasniewski.pl"
SMTP_PORT="465"
SMTP_USER="noreply@wlasniewski.pl"
SMTP_PASS=""
SMTP_FROM="noreply@wlasniewski.pl"

# ========================================
# Netlify Production (Neon Postgres) - ustaw w Netlify dashboard
# ========================================
# DATABASE_URL - zapewnia Netlify (Neon Postgres)
# JWT_SECRET = wlasniewski-fotograf-jwt-secret-2024-production
# NEXT_PUBLIC_BASE_URL = https://wlasniewski.pl
